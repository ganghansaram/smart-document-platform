# Smart Document Platform — Backend Dependencies
# Python 3.11+

# ── 웹 프레임워크 ──
fastapi==0.128.3
uvicorn==0.40.0
python-multipart==0.0.22
pydantic==2.12.5
pydantic-settings==2.13.1

# ── HTTP / 유틸 ──
requests==2.32.3
httpx>=0.27.0

# ── 검색 / RAG ──
faiss-cpu==1.12.0
sentence-transformers==5.1.2

# ── Translator (PDF 번역) ──
pdf2zh-next==2.8.2
PyMuPDF==1.25.2

# ── Explorer (문서 변환) ──
python-docx==1.2.0

# ── Ollama 클라이언트 ──
ollama==0.6.1

# ── Windows 전용 (장절번호 COM 전처리, 선택) ──
# pywin32==308
