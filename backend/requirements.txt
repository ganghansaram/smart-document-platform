# Smart Document Platform — Backend Dependencies
# Python 3.11+

# ── 웹 프레임워크 ──
fastapi==0.128.3
uvicorn==0.40.0
python-multipart==0.0.22
pydantic==2.12.5
pydantic-settings==2.13.1

# ── HTTP / 유틸 ──
requests==2.32.3
httpx>=0.27.0

# ── 검색 / RAG ──
faiss-cpu==1.12.0
sentence-transformers==5.1.2
rank-bm25==0.2.2
kiwipiepy>=0.18.0

# ── Translator (PDF 번역) ──
pdf2zh-next==2.8.2
# PyMuPDF/pymupdf4llm은 Dockerfile에서 pdf2zh-next 설치 후 강제 업그레이드
# (pdf2zh-next는 pymupdf<1.25.3 제약이지만 1.27.2에서도 정상 동작 — 웹뷰 번역 이미지 추출에 필수)

# ── Explorer (문서 변환) ──
python-docx==1.2.0
openpyxl>=3.1.0

# ── Verify (유사도 검사 — 스캔 PDF OCR) ──
rapidocr-onnxruntime==1.4.4

# ── Ollama 클라이언트 ──
ollama==0.6.1

# ── Windows 전용 (장절번호 COM 전처리, 선택) ──
# pywin32==308
