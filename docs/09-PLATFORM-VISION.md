# Smart Document Platform — 비전 및 로드맵

> **최종 갱신**: 2026-04-01
> **대상**: 한국항공우주산업(KAI) 재료공정팀(주관), 공정개발팀(협업)
> **환경**: 에어갭(Air-gapped) 사내 네트워크, 자체 호스팅 전제

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [플랫폼 현재 상태](#2-플랫폼-현재-상태)
3. [궁극적 목표](#3-궁극적-목표)
4. [지식 플랫폼 기능 로드맵](#4-지식-플랫폼-기능-로드맵)
5. [기술 기반 전략](#5-기술-기반-전략)
6. [시스템별 현황 및 방향](#6-시스템별-현황-및-방향)
7. [K-Spec 연계 전략](#7-k-spec-연계-전략)
8. [로드맵](#8-로드맵)
9. [설계 원칙](#9-설계-원칙)
10. [구조적 리스크 및 대응](#10-구조적-리스크-및-대응)
11. [플랫폼 확장 4대 방향](#11-플랫폼-확장-4대-방향)
12. [종합 현황표](#12-종합-현황표)

---

## 1. 프로젝트 개요

### 1.1 발전 경과

본 프로젝트는 "KF-21 개발 백서 열람용 웹북" 시범 시스템으로 시작하여, 약 1개월 만에 기술 문서 활용 플랫폼으로 확장되었다.

```
Phase 1 (완료)  : 정적 웹북               → "읽는 시스템"
Phase 2 (완료)  : RAG + 편집기            → "활용하는 시스템"
Phase 3 (완료)  : 관리 기능 + 통계         → "운영하는 시스템"
Phase 4 (완료)  : 비교/번역/다중 시스템     → "기술 문서 활용 플랫폼"
Phase 5 (완료)  : 품질 향상 + 지식 저장소   → "지식 활용 플랫폼"
Phase 6 (완료)  : 디자인 시스템 리파인먼트   → "세련된 플랫폼"
Phase 7 (진행중) : 비교/검증 고도화 + 전략   → "실무 검증 플랫폼"
```

### 1.2 핵심 제약

- **에어갭 환경**: 외부 인터넷 차단, 모든 구성요소 자체 호스팅
- **Vanilla JS + 모놀리식 HTML**: 프레임워크/빌드 시스템 없음
- **사용자**: 설계/품질/공정 엔지니어 (비개발직군)
- **PDF DRM**: 사내 PDF는 일정 시간 후 DRM 적용 → 번역 결과 다운로드 필수

---

## 2. 플랫폼 현재 상태

### 2.1 시스템 구성 — 3개 서브시스템 + 공통 플랫폼

```
┌─────────────────────────────────────────────────────────────┐
│                     Smart Document Platform                  │
│                                                             │
│   ┌─ 공통 플랫폼 ──────────────────────────────────────┐     │
│   │  인증 (3-role RBAC) · 설정 · 통계 · Ollama LLM     │     │
│   │  공통 헤더/푸터 · 디자인 시스템 (tokens/components)  │     │
│   └─────────────────────────────────────────────────────┘    │
│                                                             │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│   │  📖 Explorer   │  │  🔍 Compare   │  │  🌐 Translator│      │
│   │              │  │              │  │  (Notebook)   │      │
│   │  문서 탐색    │  │  문서 비교    │  │  PDF 번역     │      │
│   │  RAG 검색    │  │  규칙 검증    │  │  AI 요약/Q&A  │      │
│   │  AI 챗봇     │  │  AI 분류     │  │  마인드맵/편집 │      │
│   └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                             │
│   진입: login.html → launcher.html → 각 시스템              │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 기술 스택

| 계층 | 기술 |
|------|------|
| 프론트엔드 | Vanilla JS, CSS 디자인 시스템 (tokens.css + components.css) |
| 백엔드 | FastAPI, Python 3.11 |
| AI/ML | Ollama (gemma3, bge-m3), FAISS, Cross-encoder 리랭커 |
| 저장소 | SQLite (auth, analytics), JSON 파일 (설정, 메타데이터), 파일 시스템 (문서, PDF) |
| PDF | PDF.js v3.11.174 (legacy ES5), PyMuPDF |
| 인프라 | 폐쇄망 자립 운영, 빌드 시스템 없음 |

### 2.3 공유/독립 요소

| 요소 | 공유 | 독립 |
|------|------|------|
| 인증/세션/RBAC | O | |
| 설정 시스템 (settings.json) | O | |
| 접속 통계 | O | |
| Ollama LLM | O | |
| 디자인 시스템 (CSS 토큰) | O | |
| 플랫폼 헤더/푸터 | O | |
| 레이아웃/메인 JS | | O (시스템별) |
| API 엔드포인트 | 부분 공유 | 시스템 전용 |

---

## 3. 궁극적 목표

### 3.1 비전

> **"폐쇄망 엔지니어를 위한 개인 지식 플랫폼"**
>
> NotebookLM의 AI 문서 이해 + Obsidian의 로컬 지식 관리를 결합하되,
> **폐쇄망 동작**과 **외국어 기술문서 번역**이라는 경쟁 제품에 없는 차별점을 갖는다.

### 3.2 현재 → 목표

| 현재 | 목표 | 달성도 |
|------|------|:------:|
| Explorer: 문서 탐색 + RAG + AI 챗봇 | **지식 허브** — 문서 업로드 → AI가 이해 → 검색/질의/요약 | ✅ 완료 |
| Translator: PDF 번역 + Notebook(요약/Q&A/마인드맵/편집) | **개인 지식 저장소** — 번역 + Markdown 웹 뷰 + 챗봇 + 문서 분석 | ✅ 완료 |
| Compare: 문서 비교 + 규칙 검증 + AI 분류 | **문서 품질 게이트** — 비교 + 유사도 검사 + 규칙 검증 + 변경 분석 | 🔨 고도화 중 |
| (없음) | **K-Spec** — 표준 기술문서 제정/관리 시스템 | ⏳ 미착수 |
| (없음) | **문서 생성 시스템** — 유사 스펙 합성 → 자사 스펙 초안 생성 | ⏳ 미착수 |
| (없음) | **문서 분석 서비스** — 공통 NLP/파싱 API 레이어 | ⏳ 미착수 |

### 3.3 사용자 피드백 및 경영층 방향

**테스트 참여 동료들의 요청:**
- Translator를 **개인 문서 저장소 / 지식 저장소**로 사용하고 싶음
- 논문은 레이아웃 보존 번역, 세미나 자료/외부 문서는 **탐색 가능한 형태**로 저장
- 향후 **챗봇 연계**를 통한 개인 지식 도우미 활용 기대
- 외부 인터넷 서비스(NotebookLM, Notion)와 유사한 경험 기대

**팀장 방향:**
- **NotebookLM + Obsidian의 폐쇄망 버전** — 두 서비스의 핵심 기능 중 현업에 실질적 도움이 되는 기능을 조합
- Obsidian의 지식 그래프, NotebookLM의 AI 문서 이해를 폐쇄망 환경에서 구현
- 최종적으로 번역·챗봇·지식 관리가 하나의 시스템에서 동작

### 3.4 차별적 가치 — 왜 이 플랫폼인가

NotebookLM과 Obsidian은 각각 강력한 도구이지만, 다음을 **모두 충족하는 제품은 시장에 없다**:

| 요구사항 | NotebookLM | Obsidian | **본 플랫폼** |
|----------|:----------:|:--------:|:------------:|
| 폐쇄망(에어갭) 동작 | ❌ (Google Cloud) | ⚠️ (앱은 로컬, AI 플러그인은 클라우드) | ✅ |
| 외국어 PDF 번역 | ❌ | ❌ | ✅ |
| AI 문서 이해/챗봇 | ✅ | ⚠️ (플러그인) | ✅ (Ollama) |
| 로컬 문서 저장 | ❌ (클라우드) | ✅ | ✅ |
| 도메인 용어집 | ❌ | ❌ | ✅ |
| 기술문서 특화 (수식, 표, 규격) | ❌ | ❌ | ✅ |

### 3.5 업계 트렌드 (2024~2026)

| 트렌드 | 시사점 |
|--------|--------|
| 문서 관리 → 지식 플랫폼 전환 | 2026년까지 기업 80%가 생성형 AI 배포 전망 |
| RAG + Agentic Parsing 표준화 | 조직 문서 기반 AI 응답 생성이 엔터프라이즈 표준 |
| PDF → Markdown 전환 | Marker(GitHub 14k+), Docling(IBM) 등 PDF→MD 도구 급부상 |
| Markdown 기반 지식 관리 | Obsidian, Logseq, Notion — 모두 Markdown 기반. 편집·검색·연결에 최적 |
| 폐쇄망 AI | 전체 파이프라인(LLM + 임베딩 + 벡터DB)의 에어갭 운영 필요 |
| MBSE + Digital Thread | 방산 분야 규격 관리 현대화의 핵심 축 |
| Vanilla JS 재부상 (2025) | Netflix: React→Vanilla JS 전환 사례. 네이티브 API 발전으로 실용성 재확인 |
| **Agentic Document Processing** | 단순 RAG를 넘어 에이전트가 문서를 읽고 판단/행동. 스펙 초안 생성에 직접 적용 가능 |
| **Compliance-as-Code** | 작성 규칙을 코드화하여 CI/CD처럼 문서 작성 시 자동 검증. Compare 규칙 검증이 이 방향의 씨앗 |
| **Cross-lingual Similarity** | 다국어 임베딩(bge-m3 등)으로 한↔영 의미 유사도 비교가 실용 수준 도달. 스펙 표절 검사에 핵심 |

---

## 4. 지식 플랫폼 기능 로드맵

### 4.1 참조 제품 기능 분석

NotebookLM과 Obsidian의 핵심 기능을 분석하고, 폐쇄망 환경에서의 구현 가능성과 현업 가치를 평가한다.

#### NotebookLM (Google) — AI 기반 문서 연구 도우미

| 기능 | 설명 | 구현 | 비고 |
|------|------|:----:|------|
| 문서 업로드 | PDF, 문서 등을 소스로 등록 | ✅ | 이미 있음 (Translator 업로드) |
| AI 채팅 (문서 기반) | 업로드한 문서 기반 질의응답 | ✅ | Explorer RAG 재활용 (Ollama + FAISS) |
| 소스 근거 인용 | 답변에 출처 문단을 인용·하이라이트 | ✅ | Explorer에 이미 있음 (섹션 네비게이션) |
| 요약 생성 | 문서별 자동 요약, 브리핑 문서 | ✅ | Ollama 프롬프트로 구현 가능 |
| 커스텀 지시 | 톤, 길이, 형식 지정 | ✅ | 시스템 프롬프트 설정 |
| 다국어 출력 | 응답 언어 선택 | ✅ | Ollama 프롬프트로 가능 |
| 퀴즈/플래시카드 | 학습용 퀴즈 자동 생성 | ✅ | Ollama 프롬프트로 구현 가능 |
| 데이터 테이블 추출 | 비정형 소스에서 구조화 테이블 | ✅ | Ollama로 구현 가능 |
| 마인드맵 | 소스 내용을 시각적 맵으로 생성 | ⚠️ | D3.js 등으로 가능, 공수 큼 |
| 슬라이드/인포그래픽 | 소스 기반 프레젠테이션 자동 생성 | ⚠️ | HTML 기반 간이 버전 가능 |
| 오디오/비디오 오버뷰 | AI 호스트 팟캐스트, 요약 영상 | ❌ | TTS 모델 필요, 폐쇄망 비현실적 |
| Deep Research | 자율 에이전트의 인터넷 탐색 보고서 | ❌ | 외부 인터넷 필수 |

#### Obsidian — 로컬 지식 관리 도구

| 기능 | 설명 | 구현 | 비고 |
|------|------|:----:|------|
| Markdown 노트 | 모든 문서를 .md로 로컬 저장 | ✅ | Plan 16 Phase 5에서 번역 결과를 MD 저장 |
| 폴더 구조 | 디렉토리 기반 문서 정리 | ✅ | 이미 있음 (Translator 폴더 시스템) |
| 전문 검색 | 전체 문서 내용 검색 | ✅ | 이미 있음 (Translator 검색) |
| 마킹/하이라이트 | 텍스트 하이라이트 | ✅ | 이미 있음 (Translator 마킹) |
| 메모/주석 | 문서에 개인 메모 부착 | ✅ | 이미 있음 (Translator 메모) |
| 태그 시스템 | `#태그`로 문서 분류 | ✅ | 메타데이터에 태그 필드 추가로 구현 가능 |
| 양방향 링크 | `[[노트명]]`으로 상호 참조 | ✅ | Markdown 내 링크 파싱으로 구현 가능 |
| 백링크 | "이 문서를 참조하는 다른 문서" 목록 | ✅ | 링크 역추적 인덱스 구축 |
| 지식 그래프 | 노트 간 연결을 노드-엣지 시각화 | ⚠️ | D3.js force graph로 가능, 실용 가치는 제한적 |
| Canvas | 노트를 자유 배치하는 2D 공간 | ⚠️ | 화이트보드 엔진, 대공수 |
| 데이터베이스 뷰 | 노트를 테이블/칸반 뷰로 표시 | ⚠️ | 간이 테이블 뷰 가능, Notion DB 수준은 대공수 |
| 플러그인 생태계 | 커뮤니티 플러그인 1000+ | ❌ | 생태계 구축 불가, 핵심만 직접 구현 |
| 다중 기기 동기화 | Obsidian Sync | ❌ | 폐쇄망이라 불필요 |

### 4.2 우선순위 — 현업 가치 기준

| 순위 | 기능 | 참조 | 난이도 | 현업 가치 | 상태 |
|:----:|------|------|:------:|:---------:|:----:|
| 1 | Markdown 번역 저장 + 웹 뷰 | Obsidian | 중 | **최상** | ✅ Plan 17 |
| 2 | 개인 문서 기반 AI 챗봇 | NotebookLM | 중 | **최상** | ✅ Plan 19 |
| 3 | 문서 요약 자동 생성 | NotebookLM | 하 | 상 | ✅ Plan 19 |
| 4 | 마인드맵 | NotebookLM | 중 | 중 | ✅ Plan 20 |
| 5 | Markdown 편집기 | Obsidian | 중 | 상 | ✅ Plan 18 |
| 6 | 양방향 링크 + 백링크 | Obsidian | 하 | 상 | ⏳ 미착수 |
| 7 | 태그 시스템 | Obsidian | 하 | 중 | ⏳ 미착수 |
| 8 | 지식 그래프 시각화 | Obsidian | 중 | 중하 | ⏳ 미착수 |

> 1~5번 완료로 **Translator가 개인 지식 플랫폼 수준에 도달**. 6~8번은 문서 축적 후 실사용 피드백 기반으로 진행.

### 4.3 Markdown 전략 — 왜 HTML이 아닌 Markdown인가

번역 결과의 저장 포맷으로 HTML 대신 **Markdown**을 선택한다.

| 관점 | HTML 직접 생성 | Markdown 저장 → HTML 렌더링 |
|------|---------------|---------------------------|
| 생성 난이도 | 태그 중첩, 스타일 속성 관리 | `#`, `**`, `- ` 등 단순 문법 |
| 편집 가능성 | 사용자가 HTML 편집은 비현실적 | 텍스트 에디터로 누구나 수정 가능 |
| 외부 활용 | 브라우저에서만 열림 | Obsidian, Typora, VS Code 등 호환 |
| RAG 연계 | HTML 파싱 필요 | 텍스트 추출 거의 불필요 |
| 지식 저장소 | 플랫폼 종속 | 사용자가 .md 파일을 가져갈 수 있음 |
| 버전 관리 | diff가 태그로 오염 | 깔끔한 텍스트 diff |

**적용 범위**: Translator의 번역 결과 웹 뷰에만 Markdown 적용. Explorer의 DOCX→HTML 변환은 복잡한 서식(병합 표, MathML 수식, 이미지 크기 제어)이 필요하므로 HTML 유지.

```
Explorer: Word → HTML (유지) — 복잡한 서식의 확정 기술문서 열람
Translator: PDF 번역 → Markdown (신규) — 번역 결과의 편집·활용·지식화
```

---

## 5. 기술 기반 전략

### 5.1 핵심 결정

| 결정 | 근거 |
|------|------|
| **Vanilla JS 유지** | 1개월 만에 3개 시스템을 구현한 생산성. Netflix도 React→Vanilla JS 전환. "세련된 느낌"은 프레임워크가 아닌 디자인이 결정. 폐쇄망에서 빌드 없는 배포의 단순함 |
| **K-Spec은 React + Vite** | K-Spec은 별도 시스템으로 시작. 승인 워크플로우/Spec Tree 등 복잡한 상태 관리에 적합. 내부 도구에 Next.js는 과잉 (SEO 불필요) |
| **백엔드 FastAPI 통일** | 현재 플랫폼과 K-Spec 모두 FastAPI. 문서 분석 서비스도 FastAPI로 구축 가능 |
| **SQLite 유지, PostgreSQL은 K-Spec 도입 시** | 현재 규모(수십 명)에서 SQLite 충분. K-Spec의 Spec Tree/버전관리/워크플로우에서 PostgreSQL 필요 |
| **Big Bang 재작성 금지** | 엔터프라이즈 마이그레이션의 75%가 실패. Strangler Fig 패턴(점진적 교체)이 업계 표준 |

### 5.2 기술 스택 로드맵

```
현재 (2026 Q1)
  Frontend: Vanilla JS + CSS 디자인 시스템
  Backend:  FastAPI (단일)
  DB:       SQLite + JSON 파일
  AI:       Ollama + FAISS + bge-m3

K-Spec 시작 시 (2026 H2 예상)
  K-Spec Frontend: React + Vite
  K-Spec Backend:  FastAPI + Celery + Redis
  K-Spec DB:       PostgreSQL + Elasticsearch
  배포:            Docker Compose
  * 현재 플랫폼은 변경 없이 유지

공통 서비스 분리 시 (K-Spec 검증 후)
  문서 분석 서비스: 독립 FastAPI (port 8010)
  → 현재 플랫폼과 K-Spec 모두 HTTP API로 호출

장기 (실사용 데이터 축적 후)
  현재 플랫폼의 신규 기능을 React 모듈로 추가 (Strangler Fig)
  Vanilla JS 페이지를 필요에 따라 하나씩 전환
  두 시스템의 UI 통합 검토
```

### 5.3 왜 지금 React로 전환하지 않는가

1. **작동하는 코드를 버리는 비용** — 3개 시스템이 1개월 만에 완성, React 재작성 시 동일 기간에 절반도 불가
2. **사용자 가치 제로** — 프레임워크 전환은 사용자에게 아무 변화 없음
3. **빌드 시스템 의존성** — React는 번들러(Vite 등) 필수, 폐쇄망 배포 복잡도 증가
4. **Netflix 사례** — 오히려 React에서 Vanilla JS로 전환 (프레임워크 오버헤드 제거)
5. **"세련된 느낌"의 실체** — CSS 트랜지션, 타이포그래피, 여백, 애니메이션이 결정. 이미 디자인 시스템(tokens.css + components.css) 보유

---

## 6. 시스템별 현황 및 방향

### 6.1 Explorer — 문서 탐색 · RAG · AI 챗봇

**현재 상태**: 완성

| 기능 | 상태 |
|------|------|
| HTML 기반 구조적 문서 탐색 (트리 + 섹션 네비게이션) | ✅ |
| Word/PDF → HTML 자동 변환 (수식, 표, 이미지) | ✅ |
| 키워드 + 벡터 하이브리드 검색 (FAISS + bge-m3, RRF) | ✅ |
| RAG 챗봇 (멀티턴, 쿼리 재작성, Cross-encoder 리랭킹) | ✅ |
| 웹 편집기 (Monaco Editor) | ✅ |
| 관리자 설정 GUI | ✅ |
| 3단계 RBAC (viewer / editor / admin) | ✅ |
| 항공 용어 사전 (glossary.json) | ✅ |

**향후 방향**: Translator의 Markdown 인덱스를 Explorer RAG 파이프라인과 공유하여, 챗봇에서 번역 문서도 검색 가능하게 연동.

### 6.2 Compare — 문서 비교 · 규칙 검증 · AI 분류

**현재 상태**: 핵심 완료, 고도화 진행 중 (Plan-23)

| 기능 | 상태 |
|------|------|
| 듀얼 패널 diff (단어 수준 하이라이트) | ✅ |
| AI 의미 분류 (Ollama, 6태그 → 3그룹) | ✅ |
| 수락/거절 판정 + 벌크 처리 | ✅ |
| 규칙 검증 모드 (6종 규칙, 프리셋/커스텀) | ✅ |
| 검토 리포트 내보내기 (.txt) | ✅ |
| 스크롤바 미니맵, 텍스트 붙여넣기, 갭 정렬 | ✅ |
| **유사도 검사 모드** (크로스링구얼, bge-m3) | ⏳ Plan-23 Phase 1 |
| **내보내기 확장** (Excel/HTML) | ⏳ Plan-23 Phase 2 |
| **요구사항 문장 분류** (shall/should/may) | ⏳ Plan-23 Phase 3 |
| **다중 문서 비교** (공통/고유/충돌 매트릭스) | ⏳ Plan-23 Phase 4 |

**현재 한계**:
- DOCX 업로드: 사내 암호화 환경에서 제한적
- PDF 비교: 텍스트 추출 기반만 (시각적/레이아웃 비교는 품질 문제로 롤백 이력)
- 내보내기: .txt만 지원

**현업 요구 (신규)**:
- 카피킬러 유사 기능: 사내 스펙이 외부 영문 원문 참조로 작성되어, K-Spec 제정 시 표절 리스크 사전 점검 필요
- 한국어↔영어 크로스링구얼 의미 유사도 검사 — bge-m3 다국어 임베딩으로 폐쇄망 구현 가능

**향후 방향**:
- **유사도 검사 모드**: Compare 세 번째 모드로 추가 (비교/검증/유사도). 1:N 문서 대조, 문장 수준 유사도 분석.
- **다중 문서 비교 → 문서 생성**: 유사 스펙 3~5개 비교 → 공통 구간 추출 → LLM이 K-Spec 초안 생성. Compare가 문서 생성의 입력 파이프라인 역할.
- **K-Spec 연계**: 규칙 검증/유사도 API가 공통 서비스로 분리될 후보.

상세: `workbench/plans/23-compare-enhancement.md`

### 6.3 Translator + Notebook — PDF 번역 · 개인 지식 저장소

**현재 상태**: 완성 (개인 지식 플랫폼 수준 도달)

| 기능 | 상태 | Plan |
|------|------|------|
| PDF 업로드 + 개인 작업공간 (유저별 폴더 관리) | ✅ | 기본 |
| 듀얼 패널 뷰어 (좌=원문 PDF, 우=번역 PDF) | ✅ | 기본 |
| PDF 엔진 (pdf2zh, 레이아웃 보존) | ✅ | 기본 |
| 텍스트 엔진 (DocLayout-YOLO + Ollama, PDF 품질 우회용) | ✅ | 기본 |
| 페이지별 온디맨드 번역 (범위 최대 5페이지) | ✅ | 기본 |
| 마킹 시스템 (하이라이트 4색, 메모, 팝오버) | ✅ | 기본 |
| 선택 텍스트 AI 번역/요약 | ✅ | 기본 |
| 폴더/트리 패널 (호버 + 핀 고정, 드래그앤드롭) | ✅ | 기본 |
| 전역 검색 (본문 + 메모) | ✅ | 기본 |
| PDF HiDPI 렌더링 (devicePixelRatio 대응) | ✅ | 16 |
| 유저별 용어집 (프롬프트 주입, pdf2zh + 텍스트 엔진) | ✅ | 16 |
| PDF 다운로드 (원본 + 번역본, DRM 대응) | ✅ | 16 |
| **Markdown 웹 뷰** (번역 결과 MD 저장, 웹 렌더링) | ✅ | 17 |
| **Markdown 편집기** (Monaco, 분할뷰, 양방향 네비게이션) | ✅ | 18 |
| **문서 추출 파이프라인** (번역 없이 원문 MD 추출) | ✅ | 19 |
| **AI 요약** (크기 적응형 — 단일 패스/계층적) | ✅ | 19 |
| **Q&A 챗봇** (문서 기반, 스트리밍, 출처 페이지 배지) | ✅ | 19 |
| **마인드맵** (AI 생성, D3.js 인터랙티브) | ✅ | 20 |
| **MD 다운로드** (페이지별 + 전체 문서) | ✅ | 17 |

**번역 엔진 역할 구분**:

| 엔진 | 목적 | 출력 |
|------|------|------|
| pdf2zh (PDF) | 레이아웃 보존 번역 (논문, 인쇄/제출용) | PDF |
| 텍스트 엔진 | PDF 품질 문제 우회 (겹침, 인코딩 깨짐) | PDF (재조립) |
| **웹 뷰 (Plan 16 Phase 5)** | 읽기/검토/탐색, 지식 저장소 활용 | **Markdown** |

**발전 경과**:

```
Plan 16 (✅ 완료) : HiDPI + 용어집 + 다운로드 + 웹 뷰(Markdown)
Plan 17 (✅ 완료) : Markdown 파이프라인 (웹 뷰 렌더링 + 전체 문서 모드)
Plan 18 (✅ 완료) : Notebook 뷰어 재설계 (Monaco 편집기, 분할뷰)
Plan 19 (✅ 완료) : Notebook 플랫폼 완성 (추출 + 요약 + Q&A)
Plan 20 (✅ 완료) : Document Intelligence (마인드맵)
```

**향후 방향** — 지식 관리 확장 (문서 축적 후 진행):

| 기능 | 설명 | 전제 조건 |
|------|------|----------|
| 양방향 링크 + 백링크 | `[[문서명]]` 상호 참조, 역추적 인덱스 | MD 문서 10개+ 축적 |
| 태그 시스템 | 메타데이터 태그 필드, 태그별 필터링 | 폴더만으로 부족할 때 |
| 지식 그래프 | D3.js force graph, 문서 연결 시각화 | 링크/태그 데이터 축적 |
| 관련 문서 추천 | 자카드 유사도 기반 "비슷한 내 문서" | 문서 20개+ |

### 6.4 백엔드 API 구조

```
FastAPI (port 8000)
├── api/auth.py         — 인증 (쿠키 기반)
├── api/settings.py     — 런타임 설정
├── api/analytics.py    — 접속 통계
├── api/menu.py         — 메뉴 관리
├── api/search.py       — 검색 (키워드 + 벡터)
├── api/chat.py         — RAG 챗봇
├── api/upload.py       — 문서 업로드/변환
├── api/document.py     — 문서 CRUD
├── api/compare.py      — 문서 비교 (5개 엔드포인트)
└── api/translator.py   — PDF 번역 (30개+ 엔드포인트)
    ├── 문서 관리 (업로드, CRUD, 폴더, 검색)
    ├── PDF 번역 (페이지별, 범위별, 상태 조회)
    ├── 텍스트 번역 (폴백 엔진)
    ├── 마킹/메모 (CRUD)
    └── AI 선택 (번역/요약)
```

---

## 7. K-Spec 연계 전략

### 7.1 K-Spec 개요

K-Spec은 KAI의 표준 기술문서(규격서/스펙) 체계를 수립하는 **별도 시스템**이다.

- **Mother Spec**: 기존 분산 스펙을 통합한 표준
- **Branch Spec**: Mother에서 사업별/용도별 파생
- **Spec Tree**: 전체 스펙의 계층 관계 (L0→L1→L2→L3)

상세 설계: `docs/architecture-context.md`, `docs/K-Spec_Proposal_v0.3.docx` 참조.

### 7.2 현재 플랫폼과의 관계

| 현재 플랫폼 | K-Spec |
|------------|--------|
| **개인 엔지니어 도구** | **팀/조직 관리 시스템** |
| Vanilla JS, 파일 기반 | React + PostgreSQL + Docker |
| 번역, 탐색, 비교 | 규격 제정, 승인 워크플로우, 대시보드 |
| 운영 준비 중 | 미착수 (PoC 예정) |

**핵심 결정**: 별도 시스템으로 시작하고, **문서 분석 서비스 API로 연결**한다.

### 7.3 문서 분석 서비스 (공통 레이어)

```
┌── 현재 플랫폼 ──┐     ┌── K-Spec ──┐
│ Explorer        │     │ Spec Tree  │
│ Compare    ─────┼──┐  │ 워크플로우  │
│ Translator      │  │  │ 대시보드   │
└─────────────────┘  │  └─────┬──────┘
                     │        │
                     ▼        ▼
              ┌─────────────────────┐
              │  문서 분석 서비스     │
              │  (독립 FastAPI)      │
              │                     │
              │  문서 구조 파싱      │
              │  도메인 용어 추출    │
              │  문장 유사도 분석    │
              │  작성 규칙 검증     │
              └─────────────────────┘
```

**분리 시점**: Compare와 K-Spec이 **실제로 같은 기능을 호출하는 시점**. 지금은 YAGNI — 추상화가 이르면 실제 요구와 안 맞는 API가 만들어진다.

### 7.4 접점

| 접점 | 현재 플랫폼 측 | K-Spec 측 |
|------|--------------|-----------|
| 문서 비교 | Compare 서브시스템 | 기존 스펙 간 중복/차이 탐지 (ADR-004) |
| 용어 관리 | Explorer 용어 사전 (참조용) + Translator 용어집 (번역 제어) | 도메인 용어 자동 추출 + 사용자 사전 |
| 규칙 검증 | Compare 6종 규칙 | KAI 작성 지침 기반 검증 |

---

## 8. 로드맵

### Phase A: 현재 플랫폼 품질 향상 (2026 Q1) — ✅ 완료

```
Plan 16: HiDPI + 용어집 + 다운로드 + 웹 뷰(Markdown)  ✅
Plan 17: Markdown 파이프라인                            ✅
Plan 21: 디자인 시스템 리파인먼트                        ✅
Plan 22: 운영 안정성 강화                               ✅

달성: "쓸 만한 도구" → "인상적인 도구"
```

### Phase A+: 지식 플랫폼 기반 구축 (2026 Q1~Q2) — ✅ 완료

```
Plan 18: Notebook 뷰어 재설계 (Monaco 편집기)           ✅
Plan 19: Notebook 플랫폼 완성 (추출 + 요약 + Q&A)       ✅
Plan 20: Document Intelligence (마인드맵)                ✅

달성: "번역 도구" → "개인 지식 도우미"
```

### Phase B: Compare 고도화 + 데이터 관리 기준 (2026 Q2, 현재) — 🔨 진행 중

```
Plan 23: Compare 시스템 고도화                          🔨 계획 수립
  Phase 1: 유사도 검사 모드 (크로스링구얼, bge-m3)      ← 핵심 신규
  Phase 2: 내보내기 확장 (Excel/HTML)
  Phase 3: 규칙 검증 고도화 (shall/should/may 분류)
  Phase 4: 다중 문서 비교 + 문서 생성 파이프라인

데이터 관리 기준 수립:                                   ⏳ 계획 필요
  - 문서 ID 체계 통일 (Explorer/Translator/Compare/K-Spec 교차 참조)
  - 메타데이터 스키마 표준화
  - 파일 → DB 전환 기준 정의

목표: "기술 문서 플랫폼" → "문서 품질 게이트 + 스펙 생성 입력"
```

### Phase C: K-Spec 요구사항 + PoC (2026 Q3)

```
K-Spec 요구사항 구체화:
  - Spec Tree 계층 정의 워크숍
  - 샘플 스펙 3~5개 선정 (Mother → Branch 검증용)
  - 승인 워크플로우 설계

K-Spec PoC:
  - Docker Compose 환경 구축
  - 핵심 3모듈 (Spec Tree + 문서 편집기 + Git 버전관리)
  - Compare 유사도 API 연동 테스트

기술: K-Spec = React + Vite + PostgreSQL + Gitea
```

### Phase D: 공통 서비스 분리 (K-Spec PoC 검증 후)

```
문서 분석 서비스 독립 (독립 FastAPI, port 8010):
  - Compare의 텍스트 추출/유사도/규칙 검증 → 공통 API
  - K-Spec이 같은 API 호출
  - 문서 생성 파이프라인 공통화

분리 시점: Compare와 K-Spec이 실제로 같은 기능을 호출하는 시점
```

### Phase E: 통합 검토 (실사용 데이터 축적 후)

```
사용 패턴 기반 판단:
  - 두 시스템을 하나의 UI로 합칠 것인가?
  - Vanilla JS 페이지를 React로 전환할 것인가?
  - 어떤 페이지부터 전환할 것인가? (Strangler Fig)
```

---

## 9. 설계 원칙

### 9.1 실패하지 않는 시스템을 위한 5원칙

엔터프라이즈 마이그레이션 실패 사례에서 추출한 원칙:

| # | 원칙 | 적용 |
|---|------|------|
| 1 | **한 번에 하나만 바꿔라** (Strangler Fig) | Plan 16을 먼저 완성, K-Spec은 별도 시작 |
| 2 | **작동하는 것을 증명하고 확장해라** | K-Spec PoC에서 샘플 3~5개로 먼저 검증 |
| 3 | **공통 레이어는 필요할 때 분리해라** | 문서 분석 서비스는 실제 중복 호출이 발생할 때 |
| 4 | **매 단계에서 사용자 가치를 전달해라** | 각 Phase 완료 시 체감 가능한 변화 |
| 5 | **기술 결정은 조직 구조를 따라가라** | 개인 도구(현재)와 팀 시스템(K-Spec)은 별도 |

### 9.2 기술 원칙

- **Vanilla JS only** — 프레임워크/라이브러리 금지 (현재 플랫폼)
- **모놀리식 HTML** — 각 서브시스템은 단일 HTML 파일
- **빌드 시스템 없음** — 번들러, 트랜스파일러 미사용
- **업계 표준 방식 우선** — 검증된 패턴/도구 채택
- **과잉 설계 금지** — 현재 규모에서 필요한 것만 구현

### 9.3 디자인 시스템

CSS 토큰 기반 디자인 시스템이 전 서브시스템에 적용됨:
- `css/tokens.css` — 색상, 간격, 둥글기, 트랜지션 변수
- `css/components.css` — 버튼, 입력, 배지, 스피너 등 공통 컴포넌트
- `css/modal.css` — 모달 패턴
- 다크모드 완전 지원
- 상세: `memory/theme-guide.md`

---

## 10. 구조적 리스크 및 대응

### 10.1 현재 구조의 강점

1. **빠른 개발 속도** — 1개월 만에 3개 시스템 완성
2. **모듈화된 백엔드** — FastAPI 라우터 기반, 모듈 추가가 파일 하나 수준
3. **폐쇄망 완전 호환** — 외부 CDN/SaaS 의존 없음
4. **런타임 설정** — 재배포 없이 운영 환경 조정
5. **AI 파이프라인 확립** — RAG + 번역 + 비교 분류가 동일 Ollama 인프라 공유

### 10.2 리스크 및 대응

| 리스크 | 현재 상태 | 대응 |
|--------|----------|------|
| JS 파일 비대화 | translator.js ~5000줄, compare.html ~3500줄 | 외부 JS 파일 분리 패턴 확대 (editor-core.js, toast.js 등 이미 진행 중) |
| PDF DRM | 번역 PDF가 시간 경과 후 잠김 | Plan 16 Phase 4 다운로드 + Phase 5 Markdown으로 대응 |
| 파일 기반 동시 쓰기 | JSON 파일 동시 수정 시 충돌 가능 | 현재 규모에서는 낮은 위험. 사용자 증가 시 SQLite 확장 |
| 단일 인스턴스 | 하나의 문서 컬렉션만 운영 | 멀티 프로젝트는 K-Spec 단계에서 고려 |
| 지식 플랫폼 범위 확대 | NotebookLM/Obsidian 전체 재현 시도 위험 | 핵심 가치(챗봇 + Markdown + 링크)에 집중, 주변 기능은 실사용 피드백 후 |

### 10.3 재설계가 필요해지는 시점

- 동시 사용자 50명+ → DB 기반 세션/큐 필요
- 문서 수 1,000개+ → 파일 기반 → DB 전환 검토
- K-Spec 통합 시 → 공통 서비스 분리, 인증 체계 통합

**현재 규모에서는 재설계 불필요. 과잉 설계가 더 큰 리스크.**

---

## 11. 플랫폼 확장 4대 방향

Phase A~A+ 완료로 개인 지식 플랫폼의 기반이 갖춰졌다. 이제 남은 과제를 4개 전략 방향으로 정리한다.

### 11.1 비교/검증시스템 고도화 (즉시 착수)

**우선순위: 1순위** — 사용자가 있고 요청사항이 발생하고 있으므로 즉시 대응.

| 기능 | 목적 | 계획 |
|------|------|------|
| 유사도 검사 모드 | 외부 원문 대비 표절/유사 구간 식별 (한↔영 크로스링구얼) | Plan-23 Phase 1 |
| 내보내기 확장 | 공식 검토 리포트 (Excel/HTML) | Plan-23 Phase 2 |
| 요구사항 문장 분류 | shall/should/may 자동 분류 | Plan-23 Phase 3 |
| 다중 문서 비교 | 유사 스펙 3~5개의 공통/차이 매트릭스 | Plan-23 Phase 4 |

**핵심 기술**: bge-m3 다국어 임베딩 (이미 Explorer RAG에서 사용 중), Ollama 구조화 출력.

**K-Spec과의 관계**: Compare 고도화가 K-Spec의 사전 투자. 유사도 검사 → Mother Spec 표절 점검, 다중 비교 → 통합 초안 입력, 규칙 검증 → 작성 지침 준수.

### 11.2 문서 생성 시스템 (Compare 확장으로 구현)

**우선순위: 2순위** — Compare 다중 비교(Phase 4) 완료 후 자연스럽게 확장.

```
유사 스펙 A, B, C 업로드
    ↓
다중 문서 비교 (Phase 4)
    ↓ 공통 구간 / 고유 구간 / 충돌 구간
    ↓
[초안 생성] 버튼
    ↓ Ollama 프롬프트: "공통 구간 기반으로 KAI 표준 스펙 초안 작성"
    ↓
Markdown 초안 출력 → 다운로드 또는 편집기
```

**별도 시스템이 아닌 이유**: 입력(문서 비교)과 출력(Markdown)이 모두 기존 인프라에 있다. Compare의 다중 비교 결과를 LLM 프롬프트에 넣는 것이 전부.

**업계 시사점**: IBM DOORS/Polarion은 요구사항 *관리*는 하지만 *생성/합성*은 하지 않는다. Agentic Document Processing 트렌드와 맞물려 이 영역이 차별화 포인트가 될 수 있다.

### 11.3 K-Spec 관리시스템 (요구사항 구체화 후)

**우선순위: 3순위** — 상세 요구사항이 아직 없다. 기술적으로는 가능하지만 "무엇을 만들지"가 정의되지 않은 상태에서 코드를 짜면 재작업 불가피.

**선행 조건**:
1. Spec Tree 계층 체계 확정 (L0~L3, 실제 스펙 3~5개로 검증)
2. 승인 워크플로우 정의 (초안→리뷰→승인→배포)
3. 기존 스펙 현황 조사 (분산된 문서들의 범위 파악)

**Compare 고도화가 K-Spec을 준비하는 이유**:
- 유사도 검사 → Mother Spec 제정 시 원문 표절 점검
- 다중 비교 → 기존 유사 스펙 통합 분석
- 규칙 검증 → KAI 작성 지침 자동 검증
- 문서 생성 → K-Spec 초안 자동 생성

### 11.4 플랫폼 데이터 관리 기준 (지금 정립)

**우선순위: 1순위 (병행)** — 코드가 아니라 원칙. 대규모 개발 전에 확립해야 한다.

**현재 데이터 저장 현황**:

| 시스템 | 저장 방식 | 위치 |
|--------|----------|------|
| Explorer | SQLite (인증, 통계) + HTML (문서) + FAISS (벡터) | `data/`, `books/` |
| Translator | JSON (메타, 인덱스, 폴더) + PDF + MD | `data/translator/{user}/` |
| Compare | **무상태** (휘발성) | 없음 |
| 설정 | JSON | `data/settings.json` |

**확립해야 할 기준**:

| 기준 | 내용 | 영향 |
|------|------|------|
| **문서 ID 체계** | 시스템 간 교차 참조 가능한 통일 ID (UUID 또는 네임스페이스:ID) | 모든 시스템 |
| **메타데이터 스키마** | 공통 필드 (title, created_at, author, tags, format) 표준화 | Translator, K-Spec |
| **저장 계층 전략** | 파일 → SQLite → PostgreSQL 전환 기준 (문서 수, 동시 사용자 수) | 장기 |
| **백업/복원 단위** | 유저별 내보내기/가져오기 범위 정의 | 운영 |

**판단 기준 (전환 트리거)**:
- 동시 사용자 50명+ → DB 기반 세션/큐
- 문서 수 1,000개+ → JSON → SQLite/PostgreSQL
- K-Spec 통합 시 → 공통 서비스 분리, 인증 체계 통합

### 11.5 권장 실행 순서

```
[즉시]  Compare 고도화 (Plan-23)  ←── 사용자 요청 대응
[병행]  데이터 관리 기준 문서 작성  ←── 아키텍처 원칙 확립
  ↓
[Q3]   K-Spec 요구사항 구체화     ←── Spec Tree 워크숍, 샘플 스펙
  ↓
[Q3~4] K-Spec PoC + 문서 생성 PoC ←── Compare 다중 비교가 입력
  ↓
[Q4]   공통 문서 분석 서비스 분리  ←── 실제 중복 호출 발생 시점
```

### 11.6 제안 아키텍처 (최종 목표 상태)

```
┌─────────────────────────────────────────────────────────────────┐
│                    Smart Document Platform                       │
│                                                                 │
│  ┌─ 공통 플랫폼 ────────────────────────────────────────────┐   │
│  │  인증 (RBAC) · 설정 · 통계 · 디자인 시스템 · 데이터 관리  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌──────────┐ ┌──────────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ Explorer  │ │   Compare     │ │Translator│ │   K-Spec     │   │
│  │          │ │              │ │(Notebook) │ │  (Phase C~)  │   │
│  │ 문서 탐색 │ │ 1:1 비교     │ │ PDF 번역  │ │ Spec Tree   │   │
│  │ RAG 검색  │ │ 유사도 검사  │ │ AI 요약   │ │ 워크플로우   │   │
│  │ AI 챗봇  │ │ 규칙 검증    │ │ Q&A 챗봇  │ │ 대시보드    │   │
│  │          │ │ 다중 비교    │ │ 마인드맵   │ │ 문서 비교   │   │
│  │          │ │ 문서 생성    │ │ 편집기    │ │             │   │
│  └────┬─────┘ └──────┬───────┘ └────┬─────┘ └──────┬───────┘   │
│       │              │              │              │            │
│       ▼              ▼              ▼              ▼            │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │            문서 분석 서비스 (Phase D에서 분리)             │   │
│  │  텍스트 추출 · 유사도 · 임베딩 · 규칙 검증 · 용어 추출    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                   AI 인프라 (Ollama)                       │   │
│  │  LLM (gemma3) · 임베딩 (bge-m3) · 리랭커 (bge-reranker)  │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 12. 종합 현황표

### 12.1 시스템별 완료/진행/미착수 현황

#### Explorer

| 기능 | 상태 |
|------|:----:|
| HTML 기반 구조적 문서 탐색 (트리 + 섹션 네비게이션) | ✅ |
| Word/PDF → HTML 자동 변환 (수식, 표, 이미지) | ✅ |
| 키워드 + 벡터 하이브리드 검색 (FAISS + bge-m3, RRF) | ✅ |
| RAG 챗봇 (멀티턴, 쿼리 재작성, Cross-encoder 리랭킹) | ✅ |
| 웹 편집기 (Monaco Editor) | ✅ |
| 관리자 설정 GUI | ✅ |
| 3단계 RBAC (viewer / editor / admin) | ✅ |
| 항공 용어 사전 (glossary.json) | ✅ |

#### Translator + Notebook

| 기능 | 상태 |
|------|:----:|
| PDF 업로드 + 개인 작업공간 (폴더/트리) | ✅ |
| 듀얼 패널 뷰어 (PDF + 번역) | ✅ |
| PDF 엔진 (pdf2zh) + 텍스트 엔진 (YOLO + Ollama) | ✅ |
| 페이지별 온디맨드 번역 | ✅ |
| 마킹/메모 + 선택 AI 번역/요약 | ✅ |
| HiDPI + 용어집 + PDF 다운로드 | ✅ |
| Markdown 웹 뷰 + 전체 문서 모드 | ✅ |
| Markdown 편집기 (Monaco, 분할뷰) | ✅ |
| 문서 추출 파이프라인 (번역 없이 원문 MD) | ✅ |
| AI 요약 (크기 적응형) | ✅ |
| Q&A 챗봇 (스트리밍, 출처 배지) | ✅ |
| 마인드맵 (D3.js 인터랙티브) | ✅ |
| 양방향 링크 + 백링크 | ⏳ |
| 태그 시스템 | ⏳ |
| 지식 그래프 시각화 | ⏳ |

#### Compare

| 기능 | 상태 |
|------|:----:|
| 듀얼 패널 diff (단어 수준, 갭 정렬) | ✅ |
| AI 의미 분류 (6태그 → 3그룹) | ✅ |
| 수락/거절 + 벌크 처리 | ✅ |
| 규칙 검증 모드 (6종, 프리셋/커스텀) | ✅ |
| 리포트 내보내기 (.txt) | ✅ |
| 미니맵, 텍스트 붙여넣기, 동기 스크롤 | ✅ |
| **유사도 검사 모드** (크로스링구얼) | ⏳ Plan-23 |
| **내보내기 확장** (Excel/HTML) | ⏳ Plan-23 |
| **요구사항 문장 분류** (shall/should/may) | ⏳ Plan-23 |
| **다중 문서 비교** (공통/고유/충돌 매트릭스) | ⏳ Plan-23 |
| **문서 생성** (다중 비교 → LLM 초안) | ⏳ Plan-23 |

#### 공통 플랫폼

| 기능 | 상태 |
|------|:----:|
| 인증/세션/3단계 RBAC | ✅ |
| 런타임 설정 시스템 | ✅ |
| 접속 통계 | ✅ |
| CSS 디자인 시스템 (tokens + components) | ✅ |
| 플랫폼 헤더/푸터 | ✅ |
| 원자 쓰기 + graceful shutdown | ✅ |
| 헬스체크 + 백업 | ✅ |
| 관리자 설정 Launcher 통합 | ⏳ |
| 데이터 관리 기준 수립 | ⏳ |

#### 미착수 시스템

| 시스템 | 상태 | 선행 조건 |
|--------|:----:|----------|
| K-Spec 관리시스템 | ⏳ | 상세 요구사항, Spec Tree 체계 확정 |
| 문서 분석 서비스 (공통 API) | ⏳ | Compare/K-Spec 실제 중복 호출 발생 |

### 12.2 계획서 현황

| # | 계획 | 상태 |
|---|------|:----:|
| 1 | 폴백 번역 엔진 | ✅ 완료 |
| 2 | 스캔 PDF 겹침 테스트 | ✅ 완료 |
| 3 | 마킹 Phase 1 테스트 | ✅ 완료 |
| 4 | 마킹 동기화 타당성 | ✅ 완료 |
| 5 | AI 선택 메뉴 | ✅ 완료 |
| 6 | 플랫폼 리뷰 | ✅ 완료 |
| 7 | Translator UX | ✅ 완료 |
| 8 | PPT 변환기 | ⏳ 보류 |
| 9 | 지식 그래프 | ⏳ 보류 |
| 10 | RAG 챗봇 고도화 | ✅ 완료 |
| 11 | 챗봇 피드백 루프 | ✅ 완료 |
| 12 | RAG 품질 개선 | ✅ 완료 |
| 13 | 문서 비교 연구 | ✅ 완료 |
| 14 | 디자인 시스템 | ✅ 완료 |
| 15 | 개발 워크플로우 자동화 | ✅ 완료 (Phase 4 보류) |
| 16 | Translator 품질 향상 | ✅ 완료 |
| 17 | Knowledge Markdown Pipeline | ✅ 완료 |
| 18 | Notebook 뷰어 재설계 | ✅ 완료 |
| 19 | Notebook 플랫폼 완성 | ✅ 완료 |
| 20 | Document Intelligence | ✅ 완료 |
| 21 | 디자인 시스템 리파인먼트 | ✅ 완료 |
| 22 | 운영 안정성 강화 | ✅ 완료 |
| **23** | **Compare 고도화** | **🔨 계획 수립** |

---

## 참고 문서

| 문서 | 위치 | 내용 |
|------|------|------|
| Plan 23: Compare 고도화 | `workbench/plans/23-compare-enhancement.md` | 유사도 검사, 내보내기, 다중 비교, 문서 생성 |
| Compare 시스템 상세 | `docs/11-COMPARE-SYSTEM.md` | 비교 서브시스템 기술 문서 |
| 의미비교 연구 | `docs/research-semantic-comparison.md` | 한국어 기술문서 의미분류 연구 주제 |
| K-Spec 아키텍처 컨텍스트 | `docs/architecture-context.md` | K-Spec + 문서 분석 서비스 + NLP 벤치마크 |
| K-Spec 제안서 | `docs/K-Spec_Proposal_v0.3.docx` | 초기 아키텍처 설계 (UI 와이어프레임 포함) |
| 테마 가이드 | `memory/theme-guide.md` | 디자인 시스템 기준서 |
| 백로그 | `workbench/plans/backlog.md` | 미착수/보류 항목 모음 |

---

*최종 갱신: 2026-04-01*
