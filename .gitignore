# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
.pytest_cache/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
logs/

# Backups
backups/

# Temporary files
*.tmp
*.temp
*.bak
*.old

# ML Models (large binary files)
models/

# Offline packages (pip wheel cache)
backend/packages/

# Vector index (build artifacts)
data/vector-index.faiss
data/vector-index_meta.json

# 재색인 관측 메타 (upload.py _record_reindex_meta — 시각·소요시간, PC별 런타임 상태)
data/index-meta.json

# Reader runtime data
data/reader/pdfs/
data/reader/images/
data/reader/*.json
!data/reader/_index.json

# Claude Code
.claude/

# Windows
$RECYCLE.BIN/
nul

# Runtime settings (동적 생성)
data/settings.json

# Database files
data/*.db

# PyInstaller build artifacts
tools/heading-numberer/dist/
tools/heading-numberer/build/
tools/heading-numberer/*.spec
tools/docx2html-standalone/dist/
tools/docx2html-standalone/build/
tools/docx2html-standalone/*.spec

# Large binary/media files
*.mp4
*.pdf

# Pandoc 정적 바이너리 (Plan-60 내보내기 — 폐쇄망 배포 tar 에만 동봉, 리포 제외)
tools/pandoc/pandoc-*
# PoC 작업 산출물 (pandoc 바이너리·렌더 이미지 등)
workbench/poc-pandoc/

# Patch folders
_patch_*/

# Translator runtime data
data/translator/

# Author 저작 문서 런타임 데이터 (Plan-72 P4) — 소유권 인덱스·문서 본문은 배포별 상태
data/authored/*
!data/authored/.gitkeep

# Test outputs
pmt_test_out/

# Backend runtime temp (업로드 처리 중 임시 파일)
backend/temp/

# Playwright MCP
.playwright-mcp/

# Workbench (test/dev artifacts)
workbench/screenshots/

# 발표자료 바이너리 (보고서 md 는 커밋, pptx 산출물은 제외)
workbench/reports/**/*.pptx

# Screenshots (test artifacts in repo root)
/*.png

# Docker runtime
.env
*.tar
release-v*/
release-v*.7z*

# 웹북 변환기 배포 산출물 (별도 exe 트랙 — 소스는 커밋, 빌드물은 제외)
tools/docx2html-standalone/webbook-*.zip
tools/docx2html-standalone/2026-*-webbook-*/
tools/docx2html-standalone/2026-*-webbook-*.7z
